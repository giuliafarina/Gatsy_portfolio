import * as React from "react";
import { Container } from "react-bootstrap";

const Bio = ({ idProp }) => {
    return (
        <Container className={container}>

        </Container>

    )
}




export default Bio;